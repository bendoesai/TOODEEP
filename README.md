# TOODEEP
general insanity and goofiness

```
conda env create -f environment.yml
conda activate toodeep

python tourney/app.py
```

flask should mention something about the localhost IP it's running on while the app is launching. The program takes in any valid scryfall query assuming your pc is beefy enough to hold every result in memory.
